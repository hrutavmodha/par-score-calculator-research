\documentclass[12pt, letterpaper, fleqn]{article}
\usepackage[utf8]{inputenc}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{geometry}

\geometry{margin=1.25in}
\setlength{\mathindent}{0pt}


\title{\Huge \textbf{Deterministic Mathematical Framework for Rain-Interrpted Limited-Overs Cricket Matches}}
\author{\Large \textbf{Hrutav Modha}}
\date{\today}

\begin{document}

\maketitle

\begin{abstract}
\indent This paper introduces a deterministic mathematical framework, designed to map the resource structure of a cricket innings into a fixed 200-point capacity matrix. The model accounts for the interdependency of overs and wickets through a cross-coupling adjustment mechanism. By representing the innings as a finite resource capacity, the method provides a format-consistent methodology for both T20 and ODI matches, ensuring mathematical stability through a dynamic stabilizer $D$ and a mutable parameter $N$ to accommodate multiple interruptions.
\end{abstract}

\newpage
\tableofcontents
\newpage

\section{Prerequisite Theory}

\subsection{Scope and Prerequisites}

\indent Readers are expected to possess a firm and nuanced understanding of limited-overs cricket mechanics, specifically within the context of the T20 and ODI formats. This includes the fundamental interplay between consumed overs, wickets lost, fielding restrictions (Powerplays), and the physical constraints of a scheduled innings. The reader must understand that these two primary resources---overs and wickets---are not independent variables operating in isolation. A team cannot consume overs without exposing wickets to risk, and conversely, a team cannot preserve wickets without the unavoidable passage of time (overs). 

\indent Mathematically, the reader should be familiar with the application of piecewise continuous step functions and discrete summation over bounded intervals. A working knowledge of the Duckworth-Lewis-Stern (DLS) method, at minimum at the conceptual and philosophical level, is assumed for the comparative sections of this paper. The reader should be prepared to analyze the innings not as a statistical distribution of probable outcomes, but as a finite resource capacity.

\subsection{Rain Delays in Cricket}

\indent Cricket is a sport inherently bound by the dual constraints of time and weather. A full T20 innings is scheduled for 20 overs; a full ODI innings for 50 overs. When rain interrupts a match, the scheduled overs ($N$) are reduced either prospectively (before play resumes) or retrospectively (at the moment the innings is closed by the umpires). 

\indent This truncation breaks the linear progression of the game in a manner that affects both teams asymmetrically. When rain interrupts the first innings, the team batting first loses scheduled overs that they may have exploited for aggressive scoring, particularly in the ``death'' phase where run rates are historically at their peak. When rain interrupts the second innings, the chasing team is either penalized (fewer overs to chase) or advantaged (target already set, but a small, high-intensity chase remains). These two scenarios require fundamentally different mathematical treatments, and the failure to separate them is a methodological limitation in naive target revision systems. 

\indent Multiple rain interruptions compound this asymmetry, creating a diminishing resource capacity. After each interruption, the capacity of the match decreases. Any formula that does not dynamically track the current scheduled maximum ($N$) will operate on a flawed mathematical baseline, as overs that have been washed out must be strictly deducted from the calculation to preserve the integrity of the resource ratio.

\subsection{The Role of the Par Score}

\indent Because a truncated match features physically asymmetric resources, comparing raw run totals is mathematically invalid and structurally unfair. A team that bats 15 overs on a full-length pitch is not comparable to a team that bats 15 overs knowing the innings will close at exactly that point. The knowledge of the endpoint changes the ``resource deployment strategy'' of the batting side.

\indent A Par Score is the minimum score the chasing team must reach at any snapshot in time to be mathematically tied with the first team, rigorously accounting for overs consumed and wickets lost at the time of interruption. It serves as the ``equilibrium point'' that balances the resource consumption between the two competing innings, ensuring that the disadvantage of an interruption is shared as fairly as possible according to the deterministic laws of the sport's structure.

\section{Methodology}

\subsection{The Governing Equation}

\indent The Par Score at any moment of interruption is calculated using the following primary governing equation:

\begin{equation}
Par = Score_A \times \left( \frac{R_{used,2} + D}{R_{used,1} + D} \right)
\end{equation}

\indent In this formula, $Score_A$ represents the final total achieved by Team A. $R_{used,1}$ is the total resource footprint consumed by the team batting first at the conclusion of their innings, $R_{used,2}$ is the resource footprint consumed by the chasing team at the moment of interruption, and $D$ is the dynamic stabilizer variable. This ratio represents the ``transfer of value'' from the first innings to the second based on the proportion of the total capacity utilized by each team.

\subsection{Description:, Definitions, and Mathematical Statements}

\indent The methodology replaces historical statistical probability with a deterministic resource model. The key definitions required for the implementation of this method are as follows:

\begin{itemize}
\item \textbf{$R_{used,1}$}: The exact mathematical footprint consumed by Team A. For a completed, uninterrupted first innings, this always equals $P_{total} = 200$, making the stabilizer $D = 0$ and simplifying the Par formula to a direct resource ratio.
\item \textbf{$R_{used,2}$}: The exact mathematical footprint consumed by Team B at the moment of interruption. This includes both the ``active'' consumption (overs bowled and wickets lost) and the ``passive'' forfeiture (the destruction of future potential capacity caused by the current state of the match).
\item \textbf{$D$}: The dynamic stabilizer, an engineered offset designed to prevent the Par ratio from producing an asymptotic result when Team A's innings was itself interrupted and their $R_{used,1}$ is significantly below the 200-point ceiling.
\item \textbf{$N$}: The current scheduled maximum overs for the match. Unlike traditional methods that treat this as a constant, the proposed method treats $N$ as a mutable state variable that must be updated at every snapshot of the match.
\end{itemize}

\indent Unlike exponential probability curves fitted to historical averages, this approach bounds the mathematical output within strict physical capacities defined by the dimensions of the sport itself.

\section{Exploring Existing Methods}

\subsection{The Run Rate Method}

\indent The Average Run Rate (ARR) method was the earliest attempt at target revision. It calculates the required target by multiplying Team A's run rate by the number of overs available to Team B:

\begin{equation}
Target_{ARR} = \left( \frac{Score_A}{N_A} \right) \times N_B + 1
\end{equation}

\indent This is a single-variable linear equation that operates entirely in the dimension of time (overs), treating wickets as irrelevant. A team that scores 180/2 and a team that scores 180/9 in the same number of overs generate identical targets under ARR, despite the obvious difference in their batting positions and survival capacity. The method fundamentally ignores one of the two core resources of a cricket innings: wickets.

\subsection{The Most Productive Over Method}

\indent The MPO method attempted to construct a fair target by identifying which of Team A's overs were most productive and assuming Team B would bat against equivalently productive bowling. When rain cuts overs from Team B's innings, the method revises the target downward by subtracting the runs scored in Team A's least productive overs. 

\indent This structurally punished Team B in high-scoring matches: a team that scored heavily throughout their innings, with no ``disposable'' low-run overs, would have their target revised downward only minimally, while a team that had one or two quiet periods would lose those overs cheaply. The method has no mathematical relationship to wickets whatsoever and was quickly abandoned by the ICC due to its illogical outcomes.

\subsection{The Duckworth-Lewis-Stern Method}

\indent The current ICC standard, the Duckworth-Lewis-Stern (DLS) method, models the run-scoring resources of a batting team as a function of two variables: overs remaining and wickets in hand. The resource function takes the form of an exponential decay:

\begin{equation}
Z(u, w) = Z_0(w) \left[ 1 - e^{-b(w) \cdot u} \right]
\end{equation}

\indent The philosophical core of DLS is probability: it asks, given a historical distribution of match outcomes, what is the expected run-scoring potential of a team in this state? This creates structural limitations including regression to the historical mean and the asymptotic ceiling problem.

\section{The Method}

\subsection{Establishing Total Resource Points}

\indent The fundamental axiom of our method is that a cricket innings is a finite container of potential, represented by a fixed 200-point capacity matrix ($P_{total}$). However, the allocation of these points between the two primary dimensions---overs and wickets---varies fundamentally between formats:

\indent In \textbf{T20I} cricket, the 200 points are split in an \textbf{80:20 ratio} (160 over-points, 40 wicket-points).
In \textbf{ODI} cricket, the 200 points are split in a \textbf{70:30 ratio} (140 over-points, 60 wicket-points).

\begin{center}
\renewcommand{\arraystretch}{1.5}
\begin{tabular}{|l|c|c|c|c|}
\hline
\textbf{Component} & \textbf{T20 Points} & \textbf{T20 Share} & \textbf{ODI Points} & \textbf{ODI Share} \\
\hline
Total Over Capacity $O(N)$ & 160 & 80\% & 140 & 70\% \\
\hline
Total Wicket Capacity $W_{total}$ & 40 & 20\% & 60 & 30\% \\
\hline
\textbf{Total Capacity ($P_{total}$)} & \textbf{200} & \textbf{100\%} & \textbf{200} & \textbf{100\%} \\
\hline
\end{tabular}
\end{center}

\subsubsection{Splitting Resource Ratios for T20}

\indent To analyze the sensitivity of the model to the ratio of over-capacity ($O_{total}$) to wicket-capacity ($W_{total}$), we evaluate a snapshot of a T20 match where Team A has set a target, and Team B is deep into the middle overs, having preserved most of their wickets to prepare for a death-overs charge. \\
Team A: 160/5 (20.0) \\
Team B: 105/1 (15.0) \\

\indent In this scenario ($N=20, u=15.0, w=1$), based on the T20 distribution coefficients, the consumed components are $O(15) = 90$ (which is $0.5625$ of $O_{total}$) and $W(1) = 6$ (which is $0.15$ of $W_{total}$). The general resource formula $R_{used}$ is calculated as follows:

\begin{align*}
& R_{used} = O(u) + W(w) + \frac{w}{10} [O(N) - O(u)] + \frac{u}{N} [W_{total} - W(w)] \\
& R_{used} = 0.5625 O_{total} + 0.15 W_{total} + \frac{1}{10} [O_{total} (1 - 0.5625)] + \frac{15}{20} [W_{total} (1 - 0.15)] \\
& R_{used} = 0.5625 O_{total} + 0.15 W_{total} + 0.1 [0.4375 O_{total}] + 0.75 [0.85 W_{total}] \\
& R_{used} = (0.5625 + 0.04375) O_{total} + (0.15 + 0.6375) W_{total} \\
& R_{used} = 0.60625 O_{total} + 0.7875 W_{total}
\end{align*}

\indent We now evaluate $R_{used}$ across the distribution spectrum where $O_{total} + W_{total} = 200$:

\begin{itemize}
    \item \textbf{90:10 Ratio ($O_{total}=180, W_{total}=20$):} 
    \begin{align*}
    & R_{used} = 0.60625(180) + 0.7875(20) \\
    & R_{used} = 109.125 + 15.75 = 124.875
    \end{align*}
    
    \item \textbf{80:20 Ratio ($O_{total}=160, W_{total}=40$):} 
    \begin{align*}
    & R_{used} = 0.60625(160) + 0.7875(40) \\
    & R_{used} = 97.000 + 31.50 = 128.500
    \end{align*}
    
    \item \textbf{70:30 Ratio ($O_{total}=140, W_{total}=60$):} 
    \begin{align*}
    & R_{used} = 0.60625(140) + 0.7875(60) \\
    & R_{used} = 84.875 + 47.25 = 132.125
    \end{align*}
    
    \item \textbf{60:40 Ratio ($O_{total}=120, W_{total}=80$):} 
    \begin{align*}
    & R_{used} = 0.60625(120) + 0.7875(80) \\
    & R_{used} = 72.750 + 63.00 = 135.750
    \end{align*}
    
    \item \textbf{50:50 Ratio ($O_{total}=100, W_{total}=100$):} 
    \begin{align*}
    & R_{used} = 0.60625(100) + 0.7875(100) \\
    & R_{used} = 60.625 + 78.75 = 139.375
    \end{align*}
\end{itemize}

\subsubsection{Splitting Resource Ratios for ODI}

\indent To analyze the sensitivity of the model for the 50-over format, we evaluate an equivalent physical state in an ODI match where Team A has set a target, and Team B is entering the final ten-over phase, having lost only a single wicket and maintaining a steady accumulation rate. \\
Team A: 300/5 (50.0) \\
Team B: 240/1 (40.0) \\

\indent In this scenario ($N=50, u=40.0, w=1$), based on the ODI distribution coefficients, the consumed components are $O(40) = 90$ (which is $0.6428$ of $O_{total}$) and $W(1) = 9$ (which is $0.15$ of $W_{total}$). The general resource formula $R_{used}$ is calculated as follows:

\begin{align*}
& R_{used} = O(u) + W(w) + \frac{w}{10} [O(N) - O(u)] + \frac{u}{N} [W_{total} - W(w)] \\
& R_{used} = 0.6428 O_{total} + 0.15 W_{total} + \frac{1}{10} [O_{total} (1 - 0.6428)] + \frac{40}{50} [W_{total} (1 - 0.15)] \\
& R_{used} = 0.6428 O_{total} + 0.15 W_{total} + 0.1 [0.3572 O_{total}] + 0.8 [0.85 W_{total}] \\
& R_{used} = (0.6428 + 0.03572) O_{total} + (0.15 + 0.68) W_{total} \\
& R_{used} = 0.67852 O_{total} + 0.83 W_{total}
\end{align*}

\indent Evaluating $R_{used}$ across the distribution spectrum ($O_{total} + W_{total} = 200$):

\begin{itemize}
    \item \textbf{90:10 Ratio ($O_{total}=180, W_{total}=20$):} 
    \begin{align*}
    & R_{used} = 0.67852(180) + 0.83(20) \\
    & R_{used} = 122.1336 + 16.6 = 138.7336
    \end{align*}
    
    \item \textbf{80:20 Ratio ($O_{total}=160, W_{total}=40$):} 
    \begin{align*}
    & R_{used} = 0.67852(160) + 0.83(40) \\
    & R_{used} = 108.5632 + 33.2 = 141.7632
    \end{align*}
    
    \item \textbf{70:30 Ratio ($O_{total}=140, W_{total}=60$):} 
    \begin{align*}
    & R_{used} = 0.67852(140) + 0.83(60) \\
    & R_{used} = 94.9928 + 49.8 = 144.7928
    \end{align*}
    
    \item \textbf{60:40 Ratio ($O_{total}=120, W_{total}=80$):} 
    \begin{align*}
    & R_{used} = 0.67852(120) + 0.83(80) \\
    & R_{used} = 81.4224 + 66.4 = 147.8224
    \end{align*}
    
    \item \textbf{50:50 Ratio ($O_{total}=100, W_{total}=100$):} 
    \begin{align*}
    & R_{used} = 0.67852(100) + 0.83(100) \\
    & R_{used} = 67.852 + 83.0 = 150.852
    \end{align*}
\end{itemize}

\indent The ODI analysis reveals a higher baseline resource footprint compared to T20 for the same percentage of physical completion. This is a direct consequence of the 70:30 allocation, which places greater mathematical significance on the remaining wicket-survival potential, ensuring that the Par Score remains robust against the extended strategic horizon of the 50-over format.

\subsection{Distributing Resource Points Across Over Phases in T20}

\indent The accumulated over-points $O(u)$ for T20 are defined by discrete summation:

\subsubsection{Powerplay - Overs 1 to 6}
\begin{align*}
& f_{T20}(x) = 9 \text{ points/over} \\
& \sum_{x=1}^{6} 9 = 54 \text{ points}
\end{align*}

\subsubsection{Middle Overs - Overs 7 to 15}
\begin{align*}
& f_{T20}(x) = 4 \text{ points/over} \\
& \sum_{x=7}^{15} 4 = 36 \text{ points}
\end{align*}

\subsubsection{Death Overs - Overs 16 to 20}
\begin{align*}
& f_{T20}(x) = 14 \text{ points/over} \\
& \sum_{x=16}^{20} 14 = 70 \text{ points}
\end{align*}

\vspace{1em}
\noindent \textbf{Total T20 Over Capacity:} $54 + 36 + 70 = 160$ points.

\subsection{Distributing Resource Points Across Over Phases in ODI}

\indent The accumulated over-points $O(u)$ for ODI are defined by:

\subsubsection{Powerplay - Overs 1 to 10}
\begin{align*}
& f_{ODI}(x) = 3 \text{ points/over} \\
& \sum_{x=1}^{10} 3 = 30 \text{ points}
\end{align*}

\subsubsection{Middle Overs - Overs 11 to 40}
\begin{align*}
& f_{ODI}(x) = 2 \text{ points/over} \\
& \sum_{x=11}^{40} 2 = 60 \text{ points}
\end{align*}

\subsubsection{Death Overs - Overs 41 to 50}
\begin{align*}
& f_{ODI}(x) = 5 \text{ points/over} \\
& \sum_{x=41}^{50} 5 = 50 \text{ points}
\end{align*}

\vspace{1em}
\noindent \textbf{Total ODI Over Capacity:} $30 + 60 + 50 = 140$ points.

\subsection{Distributing Resource Points Across Wicket Tiers in T20}

\indent Wicket points $W(w)$ for T20 are defined by:

\subsubsection{Top Order}
\begin{align*}
& g_{T20}(y) = 6 \text{ points/wicket (Wickets 1--3)} \\
& \sum_{y=1}^{3} 6 = 18 \text{ points}
\end{align*}

\subsubsection{Middle Order}
\begin{align*}
& g_{T20}(y) = 4 \text{ points/wicket (Wickets 4--7)} \\
& \sum_{y=4}^{7} 4 = 16 \text{ points}
\end{align*}

\subsubsection{Tailenders}
\begin{align*}
& g_{T20}(y) = 2 \text{ points/wicket (Wickets 8--10)} \\
& \sum_{y=8}^{10} 2 = 6 \text{ points}
\end{align*}

\vspace{1em}
\noindent \textbf{Total T20 Wicket Capacity:} $18 + 16 + 6 = 40$ points.

\subsection{Distributing Resource Points Across Wicket Tiers in ODI}

\indent Wicket points $W(w)$ for ODI are defined by:

\subsubsection{Top Order}
\begin{align*}
& g_{ODI}(y) = 9 \text{ points/wicket (Wickets 1--3)} \\
& \sum_{y=1}^{3} 9 = 27 \text{ points}
\end{align*}

\subsubsection{Middle Order}
\begin{align*}
& g_{ODI}(y) = 6 \text{ points/wicket (Wickets 4--7)} \\
& \sum_{y=4}^{7} 6 = 24 \text{ points}
\end{align*}

\subsubsection{Tailenders}
\begin{align*}
& g_{ODI}(y) = 3 \text{ points/wicket (Wickets 8--10)} \\
& \sum_{y=8}^{10} 3 = 9 \text{ points}
\end{align*}

\vspace{1em}
\noindent \textbf{Total ODI Wicket Capacity:} $27 + 24 + 9 = 60$ points.

\subsection{Forfeiting Resources Logic}

\subsubsection{Forfeited Over Capacity}
\begin{equation}
F_u = \frac{w}{10} \cdot [O(N) - O(u)]
\end{equation}

\subsubsection{Forfeited Wicket Survival}
\begin{equation}
F_w = \frac{u}{N} \cdot [W_{total} - W(w)]
\end{equation}

\subsubsection{Boundary Correctness}
\indent The symmetric design ensures $R_{used} = 200$ for all completed configurations. Let the total resource formula be:
\begin{equation}
R_{used} = O(u) + W(w) + F_u + F_w
\end{equation}

\vspace{1em}
\noindent \textbf{Case 1: All out before total overs played} \\
\indent Consider a T20 match where a team is all out ($w=10$) at 15.0 overs ($u=15, N=20$):
\begin{align*}
& O(15) = 54 \text{ (PP)} + (15-6) \times 4 \text{ (Mid)} = 54 + 36 = 90 \text{ points} \\
& W(10) = 40 \text{ points} \\
& F_u = \frac{10}{10} \times [160 - 90] = 1 \times 70 = 70 \text{ points} \\
& F_w = \frac{15}{20} \times [40 - 40] = 0.75 \times 0 = 0 \text{ points} \\
& R_{used} = 90 + 40 + 70 + 0 = 200 \text{ points}
\end{align*}

\noindent \textbf{Case 2: All overs played without getting all out} \\
\indent Consider a T20 match where a team plays 20.0 overs ($u=20, N=20$) and loses 4 wickets ($w=4$):
\begin{align*}
& O(20) = 160 \text{ points} \\
& W(4) = 18 \text{ (Top)} + 1 \times 4 \text{ (Mid)} = 18 + 4 = 22 \text{ points} \\
& F_u = \frac{4}{10} \times [160 - 160] = 0 \text{ points} \\
& F_w = \frac{20}{20} \times [40 - 22] = 1 \times 18 = 18 \text{ points} \\
& R_{used} = 160 + 22 + 0 + 18 = 200 \text{ points}
\end{align*}

\noindent \textbf{Case 3: All out and all overs played} \\
\indent Consider a T20 match where a team is all out on the final ball ($u=20, w=10, N=20$):
\begin{align*}
& O(20) = 160 \text{ points} \\
& W(10) = 40 \text{ points} \\
& F_u = 1 \times [0] = 0 \text{ points} \\
& F_w = 1 \times [0] = 0 \text{ points} \\
& R_{used} = 160 + 40 + 0 + 0 = 200 \text{ points}
\end{align*}

\subsubsection{Why need Forfeiting Resources?}
\indent In the physical reality of cricket, wickets constitute the ``risk capital'' of a batting team. When a wicket is lost, the team does not merely lose one batter; they lose a proportional share of their ability to exploit all subsequent overs. A team that is 100/0 after 10 overs has a vastly higher potential energy than a team at 100/5 after 10 overs. 

\indent The forfeiture logic mathematically encodes this structural damage. It prevents the model from treating the resources as independent bins. Without forfeiture, a team finishing at 200/0 would have a different resource footprint than a team at 200/9, which would violate the law of the physical container. Forfeiture is the ``glue'' that enforces the conservation of total resource points, ensuring that every completed innings is measured against the same 200-point metric regardless of the tactical path taken.

\section{The Dynamic Stabilizer $D$}

\subsection{Derivating $D$}

\indent The stabilizer $D$ is defined by the engineered offset formula:
\begin{equation}
D = \frac{P_{total} - R_{used,1}}{k}
\end{equation}

\indent This specific formulation is selected to provide a dynamic, self-adjusting dampening effect that scales with the match's reduction. The numerator, $P_{total} - R_{used,1}$, represents the ``Residual Resource Gap''---the portion of the 200-point container that Team A was unable to utilize. By incorporating this gap into the stabilizer, the model ensures that the magnitude of $D$ is inversely proportional to the length of the first innings; the shorter the innings, the larger the stabilizer. The constant $k$ serves as the sensitivity governor, calibrating the exact degree of dampening required to maintain ratio stability without over-masking the actual performance. This structure effectively anchors the revised target to the intended scale of a full match, preventing the mathematical volatility that typically plagues raw resource ratios in hyper-truncated scenarios.

\subsubsection{Sensitivity Analysis and Selection of $k$}

\indent To determine the optimal value of the constant $k$, we evaluate an extremely high-volatility scenario where Team A finishes with $R_{used,1} = 20$ (representing a hyper-truncated 2-over burst) and $Score_A = 72$ runs. Team B has consumed $R_{used,2} = 10$ (exactly half of Team A's resource footprint).

\vspace{1em}
\noindent \textbf{Case $k=2$:}
\begin{align*}
& D = \frac{200 - 20}{2} = 90 \\
& Par = 72 \times \frac{10 + 90}{20 + 90} = 72 \times \frac{100}{110} \approx 65.4 \\
& \text{Par Score} \rightarrow 66
\end{align*}
\indent \textbf{Result:} The target of 66 runs is excessively high, nearly matching Team A's total despite Team B having only half the resources. This over-stabilization effectively erases the resource ratio.

\vspace{1em}
\noindent \textbf{Case $k=4$:}
\begin{align*}
& D = \frac{200 - 20}{4} = 45 \\
& Par = 72 \times \frac{10 + 45}{20 + 45} = 72 \times \frac{55}{65} \approx 60.9 \\
& \text{Par Score} \rightarrow 61
\end{align*}
\indent \textbf{Result:} The target of 61 runs provides a balanced equilibrium. It recognizes the high scoring rate of Team A while providing necessary protection against the extreme volatility of a 20-point footprint.

\vspace{1em}
\noindent \textbf{Case $k=6$:}
\begin{align*}
& D = \frac{200 - 20}{6} = 30 \\
& Par = 72 \times \frac{10 + 30}{20 + 30} = 72 \times \frac{40}{50} = 57.6 \\
& \text{Par Score} \rightarrow 58
\end{align*}
\indent \textbf{Result:} A target of 58 runs begins to lean too heavily toward the raw resource ratio, offering diminishing protection as the match length decreases.

\vspace{1em}
\noindent \textbf{Case $k=8$:}
\begin{align*}
& D = \frac{200 - 20}{8} = 22.5 \rightarrow 23 \\
& Par = 72 \times \frac{10 + 23}{20 + 23} = 72 \times \frac{33}{43} \approx 55.2 \\
& \text{Par Score} \rightarrow 56
\end{align*}
\indent \textbf{Result:} At 56 runs, the stabilizer fails to sufficiently dampen the volatility. The target is diverted significantly from the stable region, risking an unfair advantage for the chasing team.

\subsubsection{Optimal Selection of $k$}

\indent Based on the sensitivity analysis across extreme volatility thresholds, $k=4$ is selected as the primary constant for this method's dynamic stabilizer.

\indent The reasoning for this selection is rooted in the "Linearity-Volatility Trade-off." A lower $k$ (like 2) anchors the calculation too firmly to the 200-point ceiling, while a higher $k$ (like 8) allows the raw ratio to dominate even when the denominator is too small to be mathematically reliable. By setting $k=4$, the stabilizer $D$ acts as a perfect mathematical pivot, ensuring that the Par Score remains sensitive to actual performance while being physically bounded by the deterministic capacity of the sport's structure. This specific value ensures that as $R_{used}$ approaches zero, the target remains logically consistent with the intended intensity of the match.

\subsection{Why need $D$?}

\indent The mathematical necessity for $D$ arises from the ``Small Denominator Problem.'' In the early stages of a match, the resource footprints ($R_{used,1}$ and $R_{used,2}$) are small. In a raw ratio $\frac{R_{used,2}}{R_{used,1}}$, the gradient of the function is extremely steep. A single run or a single dot ball in a 5-over match would cause a massive swing in the Par Score, leading to an inherently unstable and unfair result.

\indent From a competitive standpoint, $D$ compensates for the ``Information Asymmetry'' inherent in target chasing. Team B always has the advantage of knowing exactly what they need to score, whereas Team A bats into an unknown future. As the match is shortened, this advantage is amplified. By adding $D$ to both the numerator and denominator, we move the calculation into a more stable linear region of the resource curve. It physically represents the ``shared uncertainty'' and anchored potential of the original 200-point container, ensuring that a 5-over match is still technically anchored to the intended scale of the sport.

\section{Handling Multiple Rain Interruptions}

\subsection{Introducing Dynamic $N$ number of Total Overs}
\begin{equation}
N_{new} = N_{current} - Overs\_Washed\_Out
\end{equation}

\subsection{Why Dynamic $N$?}
\indent Physically, each rain interruption shrinks the vessel of the match. If $N$ remains static (e.g., always 20), the $Forfeited\_Overs$ formula would calculate losses against time that no longer exists in reality. This would create ``Phantom Resources''---mathematical points that exist in the calculation but cannot be exploited on the field. Dynamic $N$ ensures that at every snapshot, the 200-point capacity is mapped exactly to the \textit{current} physical reality of the match.

\section{The Complete Resource Formula}

\begin{equation}
R_{used}(u, w, N) = \sum_{x=1}^{u} f(x) + \sum_{y=1}^{w} g(y) + \frac{w}{10} [O(N) - O(u)] + \frac{u}{N} [W_{total} - W(w)]
\end{equation}

\section{Implementation Framework for Diverse Match Scenarios}

\indent The Deterministic Framework is applied through two distinct mathematical lenses depending on the timing and nature of the interruption. These are categorized based on whether the objective is to set a new target before play (Target Revision) or to determine an equilibrium point during play (Par Score Snapshotting).

\subsection{Category A: Completed First Innings}

\subsubsection{Post-Innings Truncation (Target Revision)}
\indent \textbf{Description:} Team A completes their innings ($R_{used,1} = 200$). Rain occurs during the break, reducing Team B's overs. \\
\indent \textbf{Logic:} The target is revised by evaluating the ratio of the resource footprint Team A utilized against the resource capacity available to Team B in their new vessel.
\begin{align*}
& \text{Revised Target} = \text{Score}_A \times \left( \frac{R_{available,2}}{R_{used,1}} \right) + 1
\end{align*}
\indent Here, $R_{available,2}$ represents the total potential of the reduced vessel (Over Points + Wicket Points), mapped against the original 200-point container. As the available overs decrease, $O(N)$ for Team B shrinks, reducing $R_{available,2}$ and leading to a proportionally lower target that reflects the diminished time resource.

\indent \textbf{Example Application:} \\
Team A finishes a full T20 innings at 180/6 (20.0). During the break, rain reduces the match to a 10-over chase. \\
Team A: 180/6 (20.0) \\
Team B: Revised target for 10.0 overs.
\begin{align*}
& R_{used,1} = 200 \text{ pts (Completed Innings)} \\
& R_{available,2} = O(10) + W_{total} = (54 + 16) + 40 = 110 \text{ pts} \\
& \text{Revised Target} = 180 \times \left( \frac{110}{200} \right) + 1 = 99 + 1 = 100 \text{ runs}
\end{align*}

\subsubsection{Mid-Chase Truncation and Retrospective Termination}
\indent \textbf{Description:} Team B is already batting when rain reduces overs or ends the match. \\
\indent \textbf{Logic:} We utilize the Governing Equation with the Dynamic Stabilizer to find the equilibrium point at the moment of the snapshot.
\begin{align*}
& Par = Score_A \times \left( \frac{R_{used,2} + D}{R_{used,1} + D} \right)
\end{align*}

\indent \textbf{Example Application:} \\
Team A scores 240/10 (48.4) in an ODI. Team B is at 120/1 (22.0) when a thunderstorm ends the match. \\
Team A: 240/10 (48.4) \\
Team B: 120/1 (22.0) \\
\begin{align*}
& R_{used,1} = 200 \text{ pts (All Out)} \\
& O(22) = 30 + (22-10) \times 2 = 54 \text{ pts} \\
& W(1) = 9 \text{ pts} \\
& F_u = (1/10) \times [140 - 54] = 8.6 \text{ pts} \\
& F_w = (22/50) \times [60 - 9] = 22.44 \text{ pts} \\
& R_{used,2} = 54 + 9 + 8.6 + 22.44 = 94.04 \text{ pts} \\
& Par = 240 \times \left( \frac{94.04}{200} \right) = 112.85 \rightarrow 113 \text{ runs}
\end{align*}
\indent \textit{Verdict: Team B (120) is ahead of the Par Score (113) and wins.}

\subsection{Category B: Truncated First Innings}

\subsubsection{Premature Termination of Innings 1}
\indent \textbf{Description:} Rain stops play permanently while Team A is batting. Team B is then set a target. \\
\indent \textbf{Logic:} Because Team A was prevented from using their full 200-point capacity, the target for Team B must reflect the ratio between what Team A *actually* used and what Team B is *available* to use.
\begin{align*}
& \text{Revised Target} = Score_A \times \left( \frac{R_{available,2}}{R_{used,1}} \right) + 1
\end{align*}
\indent Here, $R_{available,2}$ is the 200-point capacity of Team B's fresh chase vessel, while $R_{used,1}$ is the footprint of Team A's truncated innings.

\indent \textbf{Example Application:} \\
In an ODI, Team A is 165/3 (35.0/50.0) when rain washes out the rest of their innings. Team B is set a target for a 35-over chase. \\
Team A: 165/3 (35.0) \\
Team B: Revised target for 35.0 overs.
\begin{align*}
& O(35) = 30 + (35-10) \times 2 = 80 \text{ pts} \\
& W(3) = 27 \text{ pts} \\
& F_u = (3/10) \times [140 - 80] = 18 \text{ pts} \\
& F_w = (35/50) \times [60 - 27] = 23.1 \text{ pts} \\
& R_{used,1} = 80 + 27 + 18 + 23.1 = 148.1 \text{ pts} \\
& R_{available,2} = 200 \text{ pts (Fresh Chase)} \\
& \text{Revised Target} = 165 \times \left( \frac{200}{148.1} \right) + 1 = 222.8 + 1 = 223 \text{ runs}
\end{align*}

\subsubsection{Mid-Play Reduction (The "Accordion" Match)}
\indent \textbf{Description:} Team A's overs are reduced mid-innings; they resume and finish the reduced innings. \\
\indent \textbf{Logic:} Team A is credited with $R_{used,1}$ calculated against the *original* $N$, including forfeiture for overs lost. Team B's target is then set using the Ratio of Totals.
\begin{align*}
& \text{Revised Target} = Score_A \times \left( \frac{R_{available,2}}{R_{used,1}} \right) + 1
\end{align*}

\indent \textbf{Example Application:} \\
Team A is 80/1 after 15.0 overs of a 50-over match. Rain reduces the match to 30 overs per side. Team A resumes and finishes their 30 overs at 200/5. \\
Team A: 200/5 (30.0) \\
Team B: Revised target for 30.0 overs.
\begin{align*}
& O(30) = 30 + (30-10) \times 2 = 70 \text{ pts} \\
& W(5) = 27 + (5-3) \times 6 = 39 \text{ pts} \\
& F_u = (5/10) \times [140 - 70] = 35 \text{ pts} \\
& F_w = (30/50) \times [60 - 39] = 12.6 \text{ pts} \\
& R_{used,1} = 70 + 39 + 35 + 12.6 = 156.6 \text{ pts} \\
& R_{available,2} = 200 \text{ pts (Fresh Chase)} \\
& \text{Revised Target} = 200 \times \left( \frac{200}{156.6} \right) + 1 = 255.4 + 1 = 256 \text{ runs}
\end{align*}

\section{Testing Formula on Real-World Data}

\indent To validate the robustness of the deterministic framework, the model was tested against historical match data, most notably the high-volatility scenarios that exposed the limitations of previous target revision systems.

\subsection{The 1992 World Cup Semi-Final Anomaly}
\indent The 1992 semi-final between South Africa and England is the most infamous failure in cricket's target revision history. Under the "Most Productive Over" (MPO) method, South Africa's target was revised from 22 runs off 13 balls to 21 runs off 1 ball after a short rain delay. We apply the deterministic framework to this snapshot to evaluate the fairness of the outcome.

\vspace{1em}
\noindent \textbf{Input Data:} \\
Team A (England): 252/6 (45.0/50.0) \\
Team B (South Africa): 231/6 (42.5/45.0)

\indent \textbf{Step 1: England's Resource Footprint ($R_{used,1}$)}
\begin{align*}
& O(45) = 30 + 60 + 25 = 115 \text{ pts} \\
& W(6) = 27 + 18 = 45 \text{ pts} \\
& F_u = 0.6 \times (140 - 115) = 15 \text{ pts} \\
& F_w = (45/50) \times (60 - 45) = 13.5 \text{ pts} \\
& R_{used,1} = 115 + 45 + 15 + 13.5 = 188.5 \text{ pts}
\end{align*}

\indent \textbf{Step 2: South Africa's Resource Footprint ($R_{used,2}$)}
\begin{align*}
& O(42.5) = 30 + 60 + 12.5 = 102.5 \text{ pts} \\
& W(6) = 45 \text{ pts} \\
& F_u = 0.6 \times (115 - 102.5) = 7.5 \text{ pts} \\
& F_w = (42.5/45) \times (60 - 45) = 14.1667 \text{ pts} \\
& R_{used,2} = 102.5 + 45 + 7.5 + 14.1667 = 169.1667 \text{ pts}
\end{align*}

\indent \textbf{Step 3: Par Score Derivation}
\begin{align*}
& D = (200 - 188.5) / 4 = 2.875 \text{ pts} \\
& Par = 252 \times \frac{169.1667 + 2.875}{188.5 + 2.875} \\
& Par = 252 \times 0.898977 = 226.54 \\
& \text{Par Score = 227 (South Africa wins)}
\end{align*}

\indent \textbf{Analysis:} At the moment the match was stopped, South Africa (231/6) was 4 runs ahead of the Par Score (227). Unlike the MPO method, which effectively deleted South Africa's most productive batting overs, the deterministic model recognizes their strong resource position and awards them the victory based on a balanced transfer of value.

\section{Match Simulations and Comparative Analysis}

\indent To further validate the model's behavior across standard rain delays and hyper-truncated interruptions, several hypothetical scenarios were simulated using the formal specification. These results demonstrate the consistency of the framework under various intensities.

\subsection{The Hyper-Extreme Burst}

\indent In this high-volatility scenario, Team A completes a hyper-truncated burst, setting a high scoring rate that Team B must now match in a single over. \\
Team A: 72/0 (2.0) \\
Team B: 0/0 (1.0) \\

\indent \textbf{Results:}
\begin{align*}
& R_{used,1} = 58.0000 \text{ pts} \\
& R_{used,2} = 29.0000 \text{ pts} \\
& D = 35.5000 \text{ pts} \\
& Par = 72 \times \frac{29.0 + 35.5}{58.0 + 35.5} = 49.63 \\
& \text{Par Score = 49.63 (Target = 50)}
\end{align*}

\indent \textbf{Analysis:} Despite Team B having half the resources, the target is 50 (not 36) because the stabilizer $D$ accounts for the massive uncertainty and information advantage in a 1-over chase.

\subsection{Standard T20 Rain Delay}
\indent Team A has completed their innings with a solid total, and Team B is halfway through their chase but currently trails the par score due to the high weight of the remaining death overs. \\
Team A: 180/5 (20.0) \\
Team B: 85/2 (10.0) \\

\indent \textbf{Results:}
\begin{align*}
& R_{used,1} = 200.0000 \text{ pts} \\
& R_{used,2} = 114.0000 \text{ pts} \\
& D = 0 \text{ pts} \\
& Par = 180 \times \frac{114.0 + 0}{200.0 + 0} = 102.60 \\
& \text{Par Score = 102.60 (Target = 103)}
\end{align*}

\indent \textbf{Analysis:} At the 10-over mark (50\% of time), a Team with 0 wickets lost would require a Par Score of only 81 runs (45\% of the chase), reflecting the heavy weighting of the upcoming Death Overs. However, the loss of 2 wickets at this juncture triggers the "Wicket-Death Coupling" penalty. Because the remaining resource container holds the high-value Death phase (14 pts/over), the forfeiture cost ($F_u$) for those 2 wickets is amplified. This pushes the resource footprint to 114 points (57\%), raising the Par Score to 103. This correctly models the tactical reality: losing wickets early in a high-intensity chase disproportionately bankrupted the team's capacity to exploit the most valuable phase of the match.

\subsection{ODI Late Match Interruption}
\indent In a high-scoring ODI, Team A sets a massive target. Team B is well-positioned with wickets in hand, but the interruption occurs just before the high-intensity final phase of the innings. \\
Team A: 320/8 (50.0) \\
Team B: 240/4 (40.0) \\

\indent \textbf{Results:}
\begin{align*}
& R_{used,1} = 200.0000 \text{ pts} \\
& R_{used,2} = 164.6000 \text{ pts} \\
& D = 0 \text{ pts} \\
& Par = 320 \times \frac{164.6 + 0}{200.0 + 0} = 263.36 \\
& \text{Par Score = 263.36 (Target = 264)}
\end{align*}

\indent \textbf{Analysis:} The Par score of 264 indicates Team B is currently trailing, as the final 10 overs of an ODI represent a high-density resource phase that requires significant scoring potential.

\subsection{The All-Out Penalty}
\indent Team A is bowled out before completing their overs, forfeiting their remaining resource potential. Team B is in a precarious position after losing several wickets, which significantly raises their required par score. \\
Team A: 160/10 (18.4) \\
Team B: 100/6 (15.0) \\

\indent \textbf{Results:}
\begin{align*}
& R_{used,1} = 200.0000 \text{ pts} \\
& R_{used,2} = 169.5000 \text{ pts} \\
& D = 0 \text{ pts} \\
& Par = 160 \times \frac{169.5 + 0}{200.0 + 0} = 135.60 \\
& \text{Par Score = 135.60 (Target = 136)}
\end{align*}

\indent \textbf{Analysis:} Because Team A was all out, they are credited with consuming 200 points. Team B, having lost 6 wickets, has a high forfeiture cost, raising their required par score to 136.

\section{Future Scopes}

\subsection{Average Run Rates}
\indent Replacing static weights with an ARR-Adaptive Engine derived from historical databases to ensure the model evolves with the increasing scoring rates of modern cricket.

\subsection{Machine Learning Based Predictions}
\indent Integration of LSTM Networks to analyze ball-by-ball momentum and adjust the deterministic resource floor based on venue-specific conditions.

\end{document}
